import { PotStatusToClassPipe } from './pot-status-to-class.pipe';

describe('PotStatusToClassPipe', () => {
  it('create an instance', () => {
    const pipe = new PotStatusToClassPipe();
    expect(pipe).toBeTruthy();
  });
});
