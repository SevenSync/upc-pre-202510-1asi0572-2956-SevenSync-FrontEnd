import { PotStatusPipe } from './pot-status.pipe';

describe('PotStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new PotStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
