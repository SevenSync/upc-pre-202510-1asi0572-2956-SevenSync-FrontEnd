import { NeedsWateringPipePipe } from './needs-watering.pipe';

describe('NeedsWateringPipePipe', () => {
  it('create an instance', () => {
    const pipe = new NeedsWateringPipePipe();
    expect(pipe).toBeTruthy();
  });
});
